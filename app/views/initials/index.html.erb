<% provide :title, "アプリの設定" %>
<h1><%= yield(:title) %></h1>
<div>
  <table class="table table-bordered">
    <tr>
      <th>アプリのタイトル</th>
      <td><%= @initial.title %></td>
    </tr>
    <tr>
      <th>アプリの説明</th>
      <td><%= simple_format @initial.description %></td>
    </tr>
    <tr>
      <th>マップのズーム<br>(0〜21)</th>
      <td><%= @initial.zoom %></td>
    </tr>
    <tr>
      <th>マップの中心の緯度<br>(-90〜90)</th>
      <td><%= @initial.lat %></td>
    </tr>
    <tr>
      <th>マップの中心の経度<br>(-180〜180)</th>
      <td><%= @initial.lng %></td>
    </tr>
  </table>
</div>

<% if !edit_mode? %>
<p>アプリの設定を変更するには、「編集モード」に切り替えます。</p>
<p><%= link_to "編集モードへ切り替え", sessions_path, method: :post, class: 'btn btn-default btn-lg' %></p>
<% end %>

<% if edit_mode? %>
<br>
<% if @initial.id.nil? %>
<%= link_to '設定の変更', new_initial_path, class: 'btn btn-primary btn-lg' %>
<% else %>
<%= link_to '設定の変更', edit_initial_path(@initial), class: 'btn btn-primary btn-lg' %>
<% end %>
<%= link_to "設定の初期化", @initial, { method: :delete, data: { confirm: '本当に初期化しますか' }, class: 'btn btn-default btn-lg' } %>
<% end %>
